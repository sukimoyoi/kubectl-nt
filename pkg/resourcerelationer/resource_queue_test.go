package resourcerelationer

import (
	"testing"
	// "github.com/sukimoyoi/kubectl-nt//pkg/relationer"
	// "github.com/stretchr/testify/assert"
)

func TestIQ(t *testing.T) {
	// kind1 := "Deployment"
	// name1 := "deploy1"
	// ns1 := "hoge"

	// kind2 := "PersistentVolume"
	// name2 := "pv1"
	// ns2 := ""

	// deploy1 := relationer.NewResource()
	// deploy1.SetBasicParams(kind1, name1, ns1)
	// RQ.Push(deploy1)
	// assert.Equal(t, 1, RQ.Len())

	// pv1 := relationer.NewResourceWithoutRelation(kind2, name2, ns2)
	// pod1 := relationer.NewResourceWithoutRelation("Pod", "pod1", "fuga")
	// tmpiq := []*relationer.Resource{pv1, pod1}
	// RQ.PushSlice(tmpiq)
	// assert.Equal(t, 3, RQ.Len())

	// r := RQ.Pop()
	// assert.Equal(t, kind1, r.Kind)
	// assert.Equal(t, name1, r.Name)
	// assert.Equal(t, ns1, r.Namespace)

	// r = RQ.Pop()
	// assert.Equal(t, kind2, r.Kind)
	// assert.Equal(t, name2, r.Name)
	// assert.Equal(t, ns2, r.Namespace)

	// assert.Equal(t, 1, RQ.Len())
}
